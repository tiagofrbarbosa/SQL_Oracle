select 
 
trunc(diferenca)*24 + trunc((diferenca - trunc(diferenca))*24) horas,
 
trunc((((diferenca - trunc(diferenca))*24) - trunc((diferenca - trunc(diferenca))*24))*60) minutos,
 
trunc((((trunc((((diferenca-trunc(diferenca))*24-trunc((diferenca-trunc(diferenca))*24)))*60)-((diferenca-trunc(diferenca))*24-trunc((diferenca-trunc(diferenca))*24))*60)*-1)*60)) segundos
 
from(
 
select data_final  - data_inicial diferenca from teste)
